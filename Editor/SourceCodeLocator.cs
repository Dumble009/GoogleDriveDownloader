using System.Diagnostics;

namespace GoogleDriveDownloader
{
    /// <summary>
    /// ソースコードのファイルパスを計算して返す機能を提供する静的クラス
    /// </summary>
    public class SourceCodeLocator
    {
        /// <summary>
        /// このメソッドを呼び出したクラスのファイルパスを返す
        /// </summary>
        /// <returns>
        /// このメソッドを呼び出したクラスのファイルパス。
        /// 絶対パスか相対パスかは保証されない。
        /// </returns>
        public static string GetSourceCodePath()
        {
            return "";
        }
    }
}